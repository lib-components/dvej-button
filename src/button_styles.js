import { css } from 'lit';

export const buttonStyle = css`
  button{
    display: block;
    font-family: 'Montserrat', sans-serif;
    font-style: bold;
    font-size: 1rem;
    border-radius: 5px;
    border: none;
    padding: 10px 15px;
    margin: 10px;
    box-shadow: 0px 8px 15px rgba(0, 0, 0, 0.1);
    cursor: pointer;
  }
`;
