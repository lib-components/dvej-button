import { css } from 'lit';

export const style = css`
button {
    display: block;
    padding: 0;
    border: none;
    margin: 0;
    background: unset;
    font-family: inherit;
    font-size: unset;
  }
  
  button{
    font-family: 'Montserrat', sans-serif;
    font-style: bold;
    border-radius: 5px;
    border: none;
    padding: 10px 15px;
    margin: 10px;
    box-shadow: 0px 8px 15px rgba(0, 0, 0, 0.1);
    cursor: pointer;
  }
`;
